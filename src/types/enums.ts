export enum Role {
  admin = 'admin',
  staff = 'staff',
}

export enum TaskStatus {
  todo = 'todo',
  in_process = 'in-process',
  review = 'review',
  completed = 'completed',
}

export enum Priority {
  low = 'low',
  medium = 'medium',
  high = 'high',
}
